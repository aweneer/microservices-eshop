server:
  port: 8080

spring:
  application:
    name: user-service

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl.defaultZone: http://localhost:8761/eureka/
    healthcheck:
      enabled: true

management:
  endpoint:
    health:
      show-details: always